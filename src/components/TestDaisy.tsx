import React from 'react';

const TestDaisy = () => {
  return (
    <div className="p-10 flex flex-col items-center gap-4">
      <button className="btn btn-primary">Botón de DaisyUI</button>
    </div>
  );
};

export default TestDaisy;
